<div class="container">
  <div class="row">
    <div class="card-panel light-blue lighten-5 col s4">
      <h4>Events</h4>
      <%= link_to new_event_path, remote: true do %>
        <button class="btn btn-default">New</button>
      <% end %></br></br>
      <div id="event-form" style="display:none;"></div>
    </div>
    </br>
    <div class="col s4 offset-s1" id="events"><%= render @events %></div>
  </div>
</div>

<%= render 'partials/reschedule_bar' %>
